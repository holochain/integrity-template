[package]
authors = [ "Holochain Core Dev Team <devcore@holochain.org>" ]
edition = "2021"
name = "coordinator"
version = "0.0.2"

[lib]
crate-type = ["cdylib", "rlib"]
name = "coordinator"

[dependencies]
integrity = {path = "../integrity"}
serde = "1"
